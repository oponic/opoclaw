# introducing Bun webview (only for vision models)

- [ ] Add tools such as click, press, type, scroll, navigate, etcetera. Model should get precise, complete control over the browser like a human user by hopefully using all the tools bun webview has
- [ ] Browser takes screenshots (after every action? model manually calls screenshots?) that shall be sent to the model
